import 'package:flutter/material.dart';

class CartPageWidget extends StatelessWidget{
  CartPageWidget(BuildContext context);

  @override
  Widget build(BuildContext context){
    return Scaffold(

    );
  }
}